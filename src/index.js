import React from "react";
import ReactDOM from "react-dom";

const Index = () => {
  return <div>
    React is Up and Running with Webpack v4
  </div>;
};

ReactDOM.render(<Index />, document.getElementById("index"));
